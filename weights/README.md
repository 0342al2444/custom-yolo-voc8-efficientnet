# Model Weights

Model weights are not stored directly in this repository.

The trained checkpoint `best.pt` should be uploaded separately through GitHub Releases instead of being committed to the Git repository.
